# Oscilloscope Notes

Use `autoprober.scope.Scope` for project scripts.

Channel 1 is the pogo measurement input. Channel 4 is the optical endstop input.

Low-level SCPI is isolated inside `autoprober.scope`.
